{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Allow the main AppRelay window to use the default Tauri IPC permissions.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
